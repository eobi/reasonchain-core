# OWASP Security Knowledge Framework — JavaScript CSRF lab.
target: http://localhost:8095/
target_type: web_api
max_steps: 25
max_depth: 3
notes: |
  Spin up locally:
      docker run --rm -d -p 8095:5000 blabla1337/owasp-skf-lab:js-csrf
  One of the many OWASP-SKF micro-labs. The CSRF variant ships a
  vulnerable client-side login flow.
